export default function Jdenticon({
  value,
  size,
  style,
  config,
}: {
  value: string;
  size?: string;
  style?: React.CSSProperties;
  config?: import("jdenticon").JdenticonConfig;
}): React.FC;
